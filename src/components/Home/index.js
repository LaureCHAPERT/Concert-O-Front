import AppHeader from 'src/components/AppHeader';
import SearchBar from 'src/components/SearchBar';

const Home = () => (
  <div>
    <AppHeader />
    <SearchBar />

  </div>
);

export default Home;
