import SearchBar from 'src/components/SearchBar';
import EventCard from '../EventCard';
import {
  Card,
} from 'react-bootstrap';
const Home = () => (
  <div>
    <SearchBar />
  </div>
);

export default Home;
